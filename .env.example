# Wallet del operador (backend + Hardhat deploy). Obtener fondos en https://faucet.0g.ai
SERVER_WALLET_PRIVATE_KEY=

# Red Galileo testnet
OG_RPC_URL=https://evmrpc-testnet.0g.ai

# Address del contrato ZegonDuel desplegado en Galileo
ZEGON_DUEL_CONTRACT_ADDRESS=

# Backend: usar inferencia 0G Compute (TEE). false = DummyZegonBrain en servidor
USE_OG_COMPUTE=true

# Cliente Vite: usar API del servidor en producción (no en localhost)
VITE_USE_OG_COMPUTE=true

# Solo desarrollo local: usar API en localhost (requiere `pnpm dev:server`)
# VITE_LOCAL_API=true

# Modelo 0G Compute (confirmar en catálogo del broker)
OG_MODEL=glm-5-fp8

# Timeout inferencia TEE (ms)
OG_INFER_TIMEOUT_MS=15000

# Address del contrato ZegonDailyPool desplegado en Galileo
ZEGON_DAILY_POOL_ADDRESS=0xF0011177988a323d2dFE4CFD29D2dFC2199F44ea

# 0G Storage indexer (turbo testnet)
OG_STORAGE_INDEXER=https://indexer-storage-turbo.0g.ai

# OG tokens a depositar en ledger Compute (mínimo ~3 OG para crear ledger)
OG_LEDGER_DEPOSIT_OG=3

# Postgres (Neon) — optional; profiles also persist via Vercel Blob when linked
DATABASE_URL=

# Auto-injected when a Blob store is linked to the Vercel project
BLOB_READ_WRITE_TOKEN=
DUEL_LOG_DIR=.duel-logs
DUEL_SESSION_DIR=.duel-sessions
LEADERBOARD_DIR=.leaderboard

# Puerto del API dev server
PORT=3000
